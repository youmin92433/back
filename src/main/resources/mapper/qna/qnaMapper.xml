<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.trycatch.mapper.qna.QnaMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_qna(individual_member_id,qna_title,qna_content,job_category_small_id,job_category_name,qna_view_count,qna_status)
        values(#{individualMemberId},#{qnaTitle},#{qnaContent},#{jobCategorySmallId},#{jobCategoryName},#{qnaViewCount},#{qnaStatus})
    </insert>
</mapper>